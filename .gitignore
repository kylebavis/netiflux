bin/
obj/
*.user
.vs/
.idea/
artifacts/
TestResults/
*.received.*

# Per-machine tool settings, not shared
.claude/settings.local.json

# Release build output
publish/
stage/
dist/

# Local state, if it ever gets copied into the working tree. Root-anchored so the
# bundled src/Netiflux/Resources/themes.json is still tracked.
/config.json
/themes.json
/saved-entries.json
